//All imported statements
/*import DAO from "./DAO/LocalStorage";
import Player from "./ULO/StartUI";
import Gameplay from "./BLO/Gameplay";

//App object (similar to main function)
const app=
{
    //Properties and fetched values
    StartUIBody : document.querySelector("#bodyStart"),
    AdditionGameplayUIBody : document.querySelector("#bodyAddGameplay"),
    SubtractionGameplayUIBody : document.querySelector("#bodySubGameplay"),
    ReportUIBody : document.querySelector("#bodyReport"),
    

    //Determine which UI object to initialize 
    init()
    {
        //Audio toggle for all pages
      /*const audioButton = document.querySelector(".fas fa-volume-up");
        const gameAudio = document.querySelector("#audioPlay");
        const isPlaying = gameAudio.play;
        const muteButton = document.createElement("i")

        audioButton.addEventListener("click",function(){
            console.log("im working")
            //Conditions to control audio toggle
            if(isPlaying==true)
            {
                gameAudio.paused();
                audioButton.style.visibility="hidden";
                muteButton.setAttribute("class","fas fa-volume-mut");  
            }

            else
            {
                gameAudio.play();
            }
        }),

        
        //Determine which UI to initialize
        if(StartUIBody != null)
        {
            this.StartUI();
        }

        else if(AdditionGameplayUIBody != null)
        {
            this.AdditionGameplayUI();
        }

        else if(SubtractionGameplayUIBody != null)
        {
            this.SubtractionGameplayUI();
        }

        else if(ReportUIBody != null)
        {
            this.ReportUI();
        }
    },


    //Load the Event Listeners for Start Page (pg3)
    StartUI() 
    {
        const playerName = document.querySelector("#userName > input");


    },

    //Load the Event Listeners for Addition Gameplay Page (pg5)
    AdditionGameplayUI() 
    {
        
    },

    //Load the Event Listeners for Subtraction Gameplay Page (pg7)
    SubtractionGameplayUI() 
    {
        
    },

    //Load the Event Listeners for ReportUI (final page)
    ReportUI() 
    {
        
    }
}
app.init();*/